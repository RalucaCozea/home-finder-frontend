
import {
} from '../actions';
import {
} from '../constants';

describe('{{ properCase name }} actions', () => {
  describe('', () => {
    it('', () => {
      const expected = {
      };
      expect().toEqual(expected);
    });
  });
});
